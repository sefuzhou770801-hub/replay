# 探索：下载完自动预审

程度：可玩，逻辑与界面已接上。本机没有 Anthropic API 密钥，没有对真实模型打通一次完整生成；启动自动路径、密钥静默、JSON 落盘与右键入口均有夹具覆盖。

## 展示位置

放在视频详情页标题栏下方、播放器上方，三行：讲什么、干货密度、可点击的重点时刻。

不放队列行展开：队列行本来就窄，三行预审会把列表撑乱；重点需要跳进播放器，详情页才是看片的地方。

## 试玩路径

1. 写入密钥（二选一即可）：
   `defaults write com.mg.replay AnthropicAPIKey -string "$ANTHROPIC_API_KEY"`
   或在启动应用的环境里设置 `ANTHROPIC_API_KEY`。
2. 用工作树构建：`REPLAY_INSTALL_APP=0 ./scripts/build_app.sh`，打开 `dist/Replay.app`。不要覆盖 `/Applications/seesee.app`。
3. 新下一条带中文字幕的视频。下载完成且目录里出现 `.zh` / `.zh-Hans` 一类字幕后，详情页顶部应出现「正在生成预审…」，随后换成三行结果。点重点时刻会 seek。
4. 旧视频：队列行右键「生成预审」。无密钥会弹出提示；无字幕会提示还没有字幕。
5. 落盘文件：`~/Movies/Replay/<uuid>.brief.json`，与字幕同前缀。
6. 启动应用不会扫历史库批量生成。

建议试玩片：https://www.youtube.com/watch?v=jNQXAC9IVRw （19 秒）。本机用 yt-dlp 拉字幕时拿到的是 `*.zh-Hans-en.vtt`（中文机翻，不是 `.zh.srt` 双语轨）。自动预审按文件名里是否含 `.zh` 判断，这条会触发；若英轨同时存在且被选中，仍以中文文件为模型输入。

测完请删自己加的队列条目。对应的 `<uuid>.*` 媒体和 `brief.json` 会随删除一起清掉。不要动既有条目。

## Token 花费估算

模型：`claude-sonnet-5`，非流式。字幕超长截到约 3 万字符。

| 片子 | 输入规模 | 粗算 |
|---|---|---|
| 本任务建议短片（约 20 秒，字幕约 400 字节） | 系统提示 + 标题作者 + 全文，合计约 500 到 800 字符 | 约 400 到 800 input token，输出约 150 到 300 |
| 顶到截断上限的长片 | 3 万字符字幕 | 约 1.5 万 input token，输出仍是一小段 JSON |

费用按当前 Sonnet 公开价粗算：短片远小于 0.01 美元；长片顶格大约 0.05 美元量级（输入为主）。具体以 Anthropic 账单为准，本环境没有真实 usage 回执。

## 自评

做好了：

- 只给本会话新完成的下载自动生成，启动补挂历史字幕不会开火。
- 无密钥自动静默；手动触发给提示。
- JSON 契约、截断、密钥优先级、中文字幕识别、右键「生成预审」有测试。
- `./scripts/test.sh` 全绿；`dist/Replay.app` 已构建（未安装到 /Applications）。

没做实的：

- 环境里没有 `AnthropicAPIKey` / `ANTHROPIC_API_KEY`，也没有对 Messages API 打出真实请求。seesee 正在运行，且和本应用共用 `com.mg.replay` 队列，没有往片库里塞夹具条目。请老板用 dist 自己下短片试。
- 探索期自写最简 `URLSession`，没有等 issue #15 的 API 底座。
- 重点时刻一行横排，片很长、重点很多时可能挤。
- 自动触发记在内存：本次下载完成且当时还没有中文字幕，关掉应用后再补挂，不会自动生成，用右键。
- 自动失败一次后不再重试，避免切回前台反复打接口。右键可再生成。

## 建议

留。转正时并进 issue #15 的调用底座（密钥、模型名、错误提示统一），并补一次带密钥的真机：短片自动生成、旧片右键、点重点跳转、确认启动不扫库。

若试玩后觉得三行占地方，可以改成默认收起、点开再看；先不要往队列行塞。
