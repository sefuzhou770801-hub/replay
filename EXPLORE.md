# 探索：盯频道自动收集

程度：可玩。核心路径已接通，未做 GUI 实机入队（避免改写用户 `queue.json`）。

## 做了什么

往添加栏粘贴频道或播放列表链接时，应用识别为订阅，弹出确认。确认后写入 `Application Support/Replay/subscriptions.json`，立刻拉一次最新 5 条，队列里没有的最多入队 3 条。之后启动时遇网络可用再查一次，并每 4 小时轮询。侧栏底部有「订阅」分组，可折叠、可删除。应用菜单有「检查订阅更新」。

判定范围：YouTube 的 `/@`、`/channel/`、`/c/`、`/user/`、`/playlist`；B 站空间与 `medialist`。带 `v=` 的观看链接（即使带 `list=`）仍当单视频。

## 试玩路径

1. 用 `REPLAY_INSTALL_APP=0 ./scripts/build_app.sh` 得到 `dist/Replay.app`，打开它，不要装到 `/Applications`。
2. 在添加栏粘贴一个更新不频繁的小频道或已完结播放列表（建议播放列表，便于核对条数）。确认「加入订阅」。
3. 侧栏应出现「订阅」分组；片刻后最多 3 条新视频进入下载队列。
4. 可用菜单「检查订阅更新」再拉一次；已在队列里的不会重复。
5. 点订阅行右侧 × 取消订阅。试完后：删掉本次入队的视频（右键删除），并删掉 `~/Library/Application Support/Replay/subscriptions.json`。不要动原有队列条目和媒体文件。

建议试玩源：`https://www.youtube.com/playlist?list=PLZHQObOWTQDPD3MizzM2xVFitgF8hE_ab`（3Blue1Brown 线性代数，已完结）。频道源如 `@CGPGrey` 仍会偶尔更新，首次最多入队 3 条。

## 自评

走得通的部分：链接判定、清单解析、按视频 URL 对照队列去重、每订阅每次最多 3 条、独立订阅文件、断网时跳过定时轮询、网络恢复后再查、确认框与侧栏删除。`./scripts/test.sh` 全绿。用本机 `yt-dlp 2026.07.04` 对真实播放列表和频道拉过清单，输出格式与解析器一致。

已知缺口：

- 首次订阅会立刻入队最多 3 条，不是「只记下当前清单、以后才收新片」。试玩能立刻看到效果，长期订阅可能不想把旧片灌进队列。
- 播放列表默认按列表顺序，已对 `/playlist` 使用 `--playlist-reverse` 取末尾 5 条。频道上传页一般是新到旧，未反转。其他站点顺序取决于 yt-dlp。
- 订阅标题用 URL 路径（`@handle` 或 list id），没有去拉频道名。
- 未做 GUI 实机入队，以免改写现有 11 条队列。

## 建议

留，但改一处再考虑进主干：首次加入订阅只记清单、不入队；之后轮询才把真正的新片入队。否则活跃频道第一次就会灌 3 条。其余（确认框、侧栏删除、4 小时、断网门控）可以保持。

若不要这个方向：弃。现有粘贴单视频路径未改。
