# 探索：跨视频字幕检索

## 做到什么程度

侧边栏添加栏下方有搜索框。⌘F 聚焦。输入关键词后，队列换成按视频分组的字幕命中（时间码、句块、上下文）。点一条会打开对应视频并跳到该句。清空搜索（输入框右侧 ×，或 Esc）回到队列。

匹配不区分大小写，并把字幕排版用的窄空格 U+2009 与普通空格当成同一个。每个视频只索引 rank 最高的那条 `.srt`（与播放器选轨一致，通常是双语 `.zh`）。首次输入时在后台扫 `~/Movies/Replay`，结果留在内存里。

未改播放器、字幕浮层和动画。片库文件只读。

## 试玩路径

1. 关掉正在跑的 seesee / Replay，避免两份进程抢写 `queue.json`。
2. 在本工作树执行：`REPLAY_INSTALL_APP=0 ./scripts/build_app.sh`，然后 `open dist/Replay.app`。
3. 按 ⌘F，或直接点侧栏搜索框。
4. 输入 `咖啡`（当前片库约 2 个视频命中）或 `节奏`（约 3 个视频）。英文可试 `system`。
5. 点任意一条，看右栏是否切到该视频并跳到该时刻。
6. 按 Esc 或点 ×，确认队列回来。

本机片库只读探针：11 个视频有字幕，`咖啡` 2 视频 / 240 句，`节奏` 3 视频 / 237 句，`system` 7 视频 / 109 句。循环歌词类视频会刷出大量相近句子，界面每条视频最多列出 40 句。

## 体验自评

能用。⌘F 和点击跳转是这条探索要验证的核心，链路是通的。

还糙的地方：搜索框占了侧栏一截固定高度，空闲时也在；命中多的视频只露前 40 句；点中后不会自动切到右栏字幕页；新下载的字幕要再搜一次（队列条数变化才会重建索引）。

## 遗留问题

- 每个视频只搜最高 rank 的那条字幕。纯英轨里的词，如果该视频已经有双语 `.zh`，仍能搜到；只有英轨独有、中文轨没有的词会漏。
- 循环/滚动字幕聚合后仍可能大量重复，没有按文本去重。
- 点中后播放会开始，不会保持暂停。
- 右栏不会跟着切到「字幕」页签，也不会滚到那一句。
- 没有高亮关键词。
- 搜索框与添加栏并存，空闲态多了一行。
- 循环歌词每视频只列出前 40 句，其余点不到。

## 建议

**留，再改一版再进主干。**

值得留：跨视频「搜一句就跳过去」是片库用起来会想要的能力，数据已经在本地字幕里，不用新模型。

进主干前建议改：

1. 空闲时把搜索收进添加栏或 ⌘F 才展开，不要常驻占高度。
2. 命中去重（相邻且文本相同的句块合并），循环歌词才不会刷屏。
3. 点中后切到右栏字幕并滚到该句。
4. 索引随字幕文件变化失效，不要只看队列条数。

若老板试完觉得「偶尔搜一下」不够常用，可以弃掉常驻入口，只留 ⌘F。
